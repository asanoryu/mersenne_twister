# Single class implementation of Mersenne Twister

Usage:

    randomiser = TwisterRand() - instantiate a randomiser object
    randomiser.seed(<int>) - seed the randomiser
    randomiser.extract_number() - get a random number
